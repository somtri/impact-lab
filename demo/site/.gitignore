node_modules/
dist/

# Dev-time copy of the WASM module -- never committed. The WASM lane's build/ is the source of
# truth; see README.md for the copy step. Window binaries under public/windows/ are committed
# (Stage 5 ship gate) -- built fresh in CI for the WASM module instead.
public/wasm/*
!public/wasm/.gitkeep
